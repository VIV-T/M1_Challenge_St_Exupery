### Main objective of the project : Create a model to predict the number of passengers per flight.
Additionnal aggregation may be expected:
- Terminal 
- Control zone : What is this exactly?
- per hour
- per day



### Mar 24/03 ###
1. Data cleaning: identify which features are relevant to train our model.
2. Check features: Get some information about unknown features, what are they? are they usefull? description? Type of data?
3. Get the data: use the script provided by Aristide to query the database and extract the needed data.
4. External data addition : use external data sources to complete our dataset. The external data sources are for now:
        - Scholar calendar and public holidays. 
        - Meteo and climat data

        => Use of timestamp column to merge the data -> define the column to use for the merge. (cf. LTScheduledDatetime or LTBlockDatetime)
        The scheduled time is more relevant (LTScheduledDatetime) => it is impossible to know the real Datetime before the real event happening, it can't be used to make prediction.
        The weather predictions must be linked to a scheduled datetime as reference.
        Same for Scholar calendar and public holidays.

        Nt: If the scholar calendar have to be renewed on a yearly basis, the weather data must be actualized in real time or either on an hourly basis.
        => use of a Airflow DAG to get the data using an API.
            -> Define the python code to do the API call for each data source
            -> use Airflow to schedule the code execution (+ data cleaning and insertion our DB)

5. Model implementation: Identify the "Label" column ("NbPax" or "NbPaxTotal"). Write a script to divide our Dataframe into Train/Validation/Test. 
    Note: important to split considering chronoligical order => Use the past data to predict future ones (or at least recent ones).


TO DO:
- Query the data : export it to csv and visualize it using data Wrangler (Ahmed) DONE
- Meteo & climat data hourly basis (using API) from meteo.data.gouv.fr + cleaning (feature selections) (Baptiste)   DONE
- Data format of Scholar calendar and public holidays data (ref. as Holidays data from now on) to suit the main dataset format. (feature "LTScheduledDatetime") - (Thibault) DONE
- Updating data profile (dataset exploration and report) => to help to determine useful features (Louis)    


Project architecture:
M1_CHALLENGE_ST_EXUPERY
    |_ .venv
    |_ config: contains the config file with our credentials
    |_ data
        |_ contains:
                - downloaded data: from Bigquery, and external sources
                - preprocessed data
                - final dataset: ready for training
                - metadata of the Airport data
    |_ scripts:
        |_ data_preparation: contains all the python script to load the data, preprocess, merge, and transformations.
        |_ training: the scripts used to split data sample and train the models.
    |_ README.md : the main file of the project, describing our work.
        


Order of execution:
1. get_main.py 
2. get_holidays.py
3. get_weather.py 
4. data_preprocessed.py  
5. data_enrichment.py


Warning : Before sending the last dataset to the training part, be sure that the index columns are removed ('unamed 1' or other column like this).

###






### Mar 25/03 ###
Main objectives today:

1. Data preparation:
    |_ add weather data preparation pipeline (B. code)
    |_ look for international holidays dataset (optional)
    |_ have a deeper understanding of the main dataset structure -> choose the column to keep/ remove, understand the different time and keep only the relevant ones.

2. Models:
    |_ initialisation script: split train / test / Validation dataset.
        |_ train-test split: follow the chronoligical order. 
        |_ sample the validation dataset from the train dataset. 
    |_ Try to run models: /!\ This is a regression problem, not a classification one.
    |_ Create a script to analyze the 1st results


3. Bonus
    |_ implement automatization of API call (using Aiflow) => daily update of main and weather data + yearly update of the holidays data


Progress:
    data preprocessing:
        Huge progress -> feature selection almost complete (maybe have to detail the choices and be precise about why we choose to keep a column or to remove it).
        Formating all the columns, except the ids and some date, the columns are now numerical (even the categorical ones) => Very important for o be able to use regression models.
        Clean architecture and pipeline. Ready for automatization (Airflow).

    training:
        First script of data preparation (split train / test / val)
        Identification of possible models to use: XGBoost, and a pytorch RNN, see: https://docs.pytorch.org/docs/stable/generated/torch.nn.LSTM.html
        First results ? 
        XGBoost : RMSE = 0.2 sur dataset de test 
        LSTM : probleme avec les 'Nan' 

    Answer from Aristide:
        Got an email from Aristide, have to review it and note all the information provided.
        Note: question him about the airport issue : destination = origin, wtf?
###


### 26/03 ###
Daily objectives:
- Data exploration : correlation calculation between features => better selection of useful features -> better model performances? (RMSE)
    - Deletion of NAN col (with missing values) => to allow to run the pytorch model (LSTM). - Or find a solution to replace missing values by a coherent one.

- Try to improve the performances of the XGBoost model (hyperaprams, data processing, ...)

- Try to run LSTM (pytorch), return metrics (like RMSE) for performances analysis

- Data profiling: complete overview of the data
    - Need of deeply understanding the data, the stakes and it structure (missing values, basic stats, ...)
    - Review of the challenge topic (paper provided) to link column to different aspect of the project:
            - NbPax prediction
            - Stream management and logistic improvement
            - Environemental and Soxial considerations

- Research other model




Issues:
- origin = destination for all the dataset ???
- management of the temporal aspect: Model cannot access to all the features for the predictions, example:
        - to predict the number of passengers of the next days, you don't have the meteo, the Pax number at check in or Scan.
        => Most important aspect to find a solution + how to still have good predictions ???

- sometimes NbPaxTotal > NbOfSeats ??? => why 
- prediction of the occupation rate instead of the number of passenger => reversible linear operation.



# Programme de l'aprem:
- Code division des dates DONE
- Finir preprocessing des données pour LSTM     DONE (main data) 
- Holidays dataset => chercher dataset international => creation d'un binary pour savoir si la zone geographique de l'aeroport de depart est en vacances ou pas.
        => La liste des pays des aeroports avec des vols en provenances de Lyon St Exupery est limité
        => Besoin des dates de zone A, B, et C en fonction de l'aeroport de depart pour les vols nationaux.
        (Vacances + Jours férié)
- Créer binaire pour les week end => (Hypothese) influence le nombre de passager -> en semaine, les passagers business sont plus important que le week-end ou des familles ou ami sont plus suceptible de prendre l'avion.
- BONUS: Base de données d'evenement locaux et internationaux (ex: concert, fetes des lumieres, ou conflit au moyen orient peuvent influencer les preicitions)


Objectif: Faire tourner LSTM + 1st results et analysis.

=> autres modeles de time series.
###




### 27/03 ###
Holidays preprocessing almost finished.
To do :     DONE
Manage the filter Arrival/Departure => 'Direction' feature. 
If 'Direction' == "Arrivée" : 
    AirportDeparture = 'AirportPrevious' or the associated variable.
Else :
    AirportDeparture = LYS (Lyon St Exupery code).


Next steps:
    - Add of B.'s preprocess (additional columns)
    - Add of additional columns (ex: number of passenger on avg regarding the past 7 fly to the same destionation)
    - Add of week end's column.
    - Modify "cache_vacances.json" location of saving (in the code)
    - Ask B. what's next (avoid git conflict and star to merge branch to centralized the work)







###